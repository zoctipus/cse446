{
    "version": "2.0.0",
    "configurations": [
        {
            "name": "Python: Current File (Debug with inv test)",
            "type": "python",
            "request": "launch",
            "program": "${file}",
            "console": "integratedTerminal",
            "justMyCode": true,
            "preLaunchTask": "inv test"
        }
    ],
}
